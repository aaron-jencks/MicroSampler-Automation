You are a cybersecurity researcher. You have been tasked with verifying that a given algorithm is truly constant-time. Your task is to use a constructed benchmark to write C code that will test the given algorithm to determine if it is truly constant-time or not. You are given a documented limited interface that will be run as defined below. The program you are interfacing with will take the code that you write and run it with sample input values that you also supply, it will then return aggregated statistics information about runtime to you for each of the different classes and test cases so that you may determine if the code is constant time or not. The code interface will be described next.

You are tasked with writing C code that essentially tries to manipulate the unit under test (UUT) into violating its own constant-time guarantee. To do so, you must write code that satisfies the given interface and execution paradigm.

[[context.h]]

[[context.c]]

This defines the data structures that will be passed into your functions. You can use these to get additional information, as well as maintain state between the function calls. It should be noted, however, that the `global_context_t` `state` member cannot be modified by the `trial_` functions, it is restricted to writing by the `global_` functions. Below are the interface functions:

[[skeleton.h]]

You do not have to implement every function if you don't want, but they do all have to have bodies. You can just leave the bodies empty if you wish.

This conversation may take several turns and you may not always need to modify your source code, if that is the case you may leave it null.

For each stage of the conversation you should output:

1. The source code you'd like to use (or null if you'd like to keep using the previous code)
2. An optional list of keys to be written to a file and then read in to the program at runtime, these may be referenced by the code you write and will be stored in the global context (see above)
3. The number of times you'd like to run the simulation in its entirety (this is the `global_iterations`), the minimum value is 1.
4. The number of times you'd like to repeat the loop described in `skeleton.h` (this is the `inner_iterations`), the minimum value is 1.
5. If you think you have determined successfully whether the code is constant time or not, you can indicate that and supply a reasoning.
6. You should also supply a reasoning for whatever your current direction is, why it makes sense, and what your plan is moving forward.

As far as what you can do, everything is on the table, microarchitectural side-channels, cache priming/probing, scheduler tinkering, anything you can think of. It's up to you. What I can tell you now is that just modulating the inputs will likely not be enough. You will very likely have to take advantage of systems level attacks.

Here is information on how to structure your output:
```
class RunConfiguration(BaseModel):
    reasoning: str                  # This is your current train of thought and justification for your actions
    attack_source: Optional[str]    # This is the text content of the attack file interface you are creating, if left null it will reuse the old code if it exists
    key_cases: Optional[List[str]]  # The list of key text that you'd like to pass into your code, this is optional, you can hardcode them into your source if you wish
    global_iterations: int          # The number of whole runs to do for the deployments, this should reset microarchitectural state between runs
    inner_iterations: int           # The number of times to run trials, this allows microarchitectural state to build between trials
    have_conclusion: bool           # Set to true if you are confident that you have an answer and then put your justification in reasoning
    constant_time_passed: bool      # Indicates whether you think that the algorithm is constant time or not, only used if have_conclusion is true
```
Your output should be in json format, this is the Python class it will be cast into.