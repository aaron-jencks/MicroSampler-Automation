```
void
br_ccopy_v2(uint32_t ctl, void *dst, void *dummy, const void *src, size_t len)
{
    if (ctl) {
        memmove(dst, src, len);
    }
    else {
        memmove(dummy, src, len);
    }
    return;
}
```

If you want to look at the project source code, it will be included in the workbench in the [[config:workbench:source:prefix]] directory. It will be stored in the `bearssl` directory. This is a read-only copy, it is not used to build the harness, modifying these files will do nothing.

The actual function can be found in [[config:workbench:source:prefix]]/bearssl/codec/ccopy.c for your perusal.